version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
  build:
    commands:
      - ls -la
      - which sam && sam --version
      - npm run deploy:${ENV_TYPE}

artifacts:
  name: ${ENV_TYPE}-build-artifacts-$(date +%Y-%m-%d)
  files:
    - .aws-sam/*
    - build/*
